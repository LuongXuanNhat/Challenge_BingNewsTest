namespace BingNewsTest
{
    internal class BingNewService
    {
        //private readonly IArticle _iArticle;

        //public BingNewService()
        //{
        //    _iArticle = new ArticleService();
        //}

        //internal AdArtile CreateAdArticle(string image, string title, DateTime postedTime, Provider provider)
        //{
        //    return new AdArtile(provider.GetProviderIcon(), provider.GetProviderName(), postedTime, title, image);
        //}

        //internal Article CreateArticle(string image, DateTime postedTime, Provider provider, string title, int likeNumber, int disLikeNumber, int commentNumber)
        //{
        //    return new Article(provider.GetProviderIcon(), provider.GetProviderName(), postedTime, title,image, likeNumber, disLikeNumber, commentNumber);
        //}

        //internal BasePost CreateBasePost(Provider provider, DateTime postedTime, string title)
        //{
        //    return new BasePost(provider.GetProviderIcon(), provider.GetProviderName(), postedTime, title);
        //}

        //internal MicrosortNotification CreateMicrosoftNotification(string topic, string image, TypeOfNotification type, string noticeFrom)
        //{
        //    return new MicrosortNotification(topic, image, type, noticeFrom);
        //}

        //internal PageResult<Article> CreatePagedResult(Pagination pagination)
        //{
        //    return _iArticle.GetArticles(pagination);
        //}

        //internal Pagination CreatePagination(int total, int index, int size)
        //{
        //    return new Pagination(total, index, size);
        //}

        //internal Provider CreateProvider(string providerIcon, string providerName)
        //{
        //    return new Provider(providerIcon, providerName);
        //}
    }
}